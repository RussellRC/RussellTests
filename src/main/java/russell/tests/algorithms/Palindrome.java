package russell.tests.algorithms;



public class Palindrome {

    public boolean isPalindrome(final String s) {
        if (s == null || s.length() < 2 || s.trim().length() < 2) {
            return false;
        }
        
        final String word = s.toLowerCase();
        
        
        return true;
    }
    
}
